/**
 * Plafond de lignes pour une recherche catalogue (UI + API `POST /catalog/products/search`).
 * Doit couvrir l’ensemble des fiches en base avec une marge de croissance.
 */
export const CATALOG_SEARCH_MAX_LIMIT = 2500;
