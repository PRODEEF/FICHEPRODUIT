/**
 * Valeur technique de `userId` pour les analyses lancées sans compte (jamais un id `auth.users` réel).
 * À garder aligné avec le filtrage côté {@link SiteAnalysisService}.
 */
export const GUEST_SITE_ANALYSIS_USER_ID =
  '00000000-0000-4000-8000-000000000001';
