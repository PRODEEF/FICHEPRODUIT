/**
 * Nombre maximal de produits par export CSV PrestaShop.
 * Aligné sur {@link SHOP_BRANDS_CATALOG_LIMIT} (catalog.service) : une boutique
 * ne charge pas plus de produits que ce plafond dans l’UI catalogue.
 */
export const PRESTASHOP_EXPORT_MAX_PRODUCTS = 1000;
